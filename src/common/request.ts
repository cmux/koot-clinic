// TODO: Request module

import axios, { Method, AxiosRequestConfig } from 'axios';

//

//

const request = (method: Method, data: any, config = {}) => {};

//

const requestDo = (config: AxiosRequestConfig) => axios(config).then(res => {});

export default request;
